
import { themeClasses } from "../../../shared/config/theme"

export const Header = () => {
    return (
        <div className={themeClasses.chatHeader}>
            Telegram Clone
        </div>
    )
}